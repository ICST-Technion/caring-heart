import 'package:test/test.dart';

void main() {
  test('1', () async {
    // var item1 = MockItem();
    // when(item1.id).thenReturn('1');
    // final inv = getInventoryMock([item1]);
    // print(item1.id);
    // print(item1.id);
    // // print((await inv.getItemByID('1')).id);
    // inv.getItemByID('1');
    // inv.getItemByID('1');

    // verify(inv.getItemByID('1')).called(2);
  });
}
